---
"@lmfaole/basics": minor
---

Change `basic-accordion` to use direct child `details` and `summary` markup instead of separate trigger and panel nodes, keeping the root element focused on accordion state rules and keyboard movement.
