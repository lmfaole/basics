---
"@lmfaole/basics": patch
---

Move the optional starter styling sources into dedicated folders under `basic-styling/`, with shared tokens in `tokens/` and component-specific CSS in `components/`.
