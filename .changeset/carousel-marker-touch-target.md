---
"@lmfaole/basics": patch
---

`basic-carousel` scroll markers now meet the 44 × 44 px touch-target minimum. The default `--basic-carousel-marker-size` is bumped from `1.75rem` to `2.75rem` so a tap reliably hits the indicator. Override the variable to restore the previous visual size.
